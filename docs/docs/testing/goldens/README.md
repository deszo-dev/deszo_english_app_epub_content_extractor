# Golden Outputs

This directory is reserved for accepted post-implementation golden outputs.

Current normalized expected outputs are kept in `docs/testing/fixtures/expected/` and pinned in `docs/contracts/fixture-manifest.json`.
