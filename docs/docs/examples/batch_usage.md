# Batch Usage Example

Source file: `examples/batch_usage.py`

The module does not need a separate batch API for v3.0. Batch processing is repeated public API calls with deterministic per-file results.
